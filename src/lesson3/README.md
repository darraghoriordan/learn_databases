These tests are designed to only run on the small database schema

Just pet_owner and pet tables present

See lesson 3 for details

You must use `yarn test:named theTestName` to specify a test to run.
